-- ============================================================
-- 0025: Время у блока дня
-- Дата: июль 2026
-- Зачем: в маршруте по дням у каждого пункта (блока) есть время
--   (11:00, 16:00, 20:00 …). Хранится ТЕКСТОМ, как всё время/даты
--   в проекте. Опционально: если пусто — на клиентской время не выводится.
-- Статус: накатить на STAGING. На PROD — НЕТ.
-- ============================================================

alter table public.day_blocks
  add column time text;