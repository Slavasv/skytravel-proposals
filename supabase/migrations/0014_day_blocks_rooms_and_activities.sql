-- ============================================================
-- 0014: day_blocks — несколько типов номеров + активности отеля
-- Дата: июль 2026
-- Зачем: карточка отеля в предложении стала как в дестинейшене:
--        можно выбрать НЕСКОЛЬКО типов номеров (семья берёт
--        Deluxe + Suite) и вписать активности отеля.
--        Старое поле room_type_ru/en остаётся, но не используется.
-- Статус: накачено на STAGING. На PROD — НЕТ.
-- ============================================================


alter table public.day_blocks
  add column room_ids uuid[] default '{}',      -- выбранные типы номеров (пусто = все)
  add column activities_ru text,
  add column activities_en text;