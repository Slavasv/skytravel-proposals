-- Рабочий email для сопоставления сотрудника с аккаунтом Microsoft (Azure/Planner).
-- Отвязан от логина входа (auth.users.email уникален на весь проект — один и тот же
-- адрес нельзя использовать логином в двух компаниях). ms_email НЕ уникален:
-- один адрес может встречаться в разных компаниях. Синхронизация исполнителя
-- берёт ms_email, а если он пуст — падает на обычный email профиля.
 
alter table public.profiles add column if not exists ms_email text;
 