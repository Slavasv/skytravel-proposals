-- ============================================================
-- 0017: Фиксация выбранного дестинейшена
-- Дата: июль 2026
-- Зачем: агент прикрепляет к запросу несколько дестинейшенов
--        («вот варианты по Африке»), клиент выбирает один.
--        is_selected отмечает выбранный. Остальные остаются
--        в истории — видно, что предлагали и что зашло.
-- Статус: накачено на STAGING. На PROD — НЕТ.
-- ============================================================

alter table public.request_proposal_links
  add column is_selected boolean not null default false;