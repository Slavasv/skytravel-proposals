-- ============================================================
-- 0027: Подзаголовок у блока библиотеки
-- Дата: июль 2026
-- Зачем: у блока (отель/активность/трансфер/город) — короткий подзаголовок
--   ПОД названием. На публичной: subtitle = короткая строка под именем,
--   а description уезжает в раскрывающийся блок «Подробнее».
--   Раньше эту роль играл description — теперь разделили.
-- Статус: накатить на STAGING. На PROD — НЕТ.
-- ============================================================

alter table public.content_blocks
  add column subtitle_ru text,
  add column subtitle_en text;