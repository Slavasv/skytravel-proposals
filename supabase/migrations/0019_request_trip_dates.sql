-- ============================================================
-- 0019: Даты поездки в запросе (глубина запроса)
-- Дата: июль 2026
-- Зачем: агент указывает, на какие даты клиент собирается лететь.
--        Вместе с created_at даёт «глубину запроса» — за сколько
--        до поездки обращаются. Нужно для аналитики и планирования.
--        Даты примерные: если клиент не знает точных, агент прикидывает.
-- Статус: накачено на STAGING. На PROD — НЕТ.
-- ============================================================

alter table public.requests
  add column trip_start date,
  add column trip_end date;